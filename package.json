{
  "name": "claudesub",
  "version": "0.1.0",
  "description": "Switch between Claude Code OAuth subscriptions on macOS.",
  "private": true,
  "type": "module",
  "bin": {
    "claudesub": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test:build": "tsc -p tsconfig.tests.json",
    "test": "npm run test:build && node --test 'dist-test/tests/**/*.test.js'"
  },
  "engines": {
    "node": ">=18.17"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  }
}
